1. Explore available methods
:FzfLua builtin # all fzf commands
2. Project Files Buffers
files = all files in current directory
	 = f1 = help
	= f2 = maximize search
	 = f4 = preview file content
		 shift + arrow = navigate in preview
	 = f5 = change win position
oldFiles = MRU files 
buffers = switch between active bufferes
3. Search and quick fix
life_grep = search in project
grep_cword = search for cursor word
quickfix = search for error
4. Git integration
git_files
git_status
git_commit
5. Language Server Protocol (LSP)
6. Miscellaneous
help = search in linux help
man = search in man
keymaps = search shortcut
command_history = vim command history

<leader>/ = grep current buffer
<leader>; = buffers
<leader>fb = buffers
<leader>fo = old files
<leader>fr = resume

